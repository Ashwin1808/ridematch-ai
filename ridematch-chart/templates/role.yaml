apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  name: ridematch-pod-reader
  namespace: ridematch

rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]